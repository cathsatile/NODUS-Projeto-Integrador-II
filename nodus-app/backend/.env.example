# DB_PATH é opcional — por padrão o arquivo SQLite fica em backend/nodus.db.
# Defina um caminho absoluto para usar outra pasta (ex: app.getPath('userData') no Electron).
# DB_PATH=/caminho/para/nodus.db
JWT_SECRET=troque_por_um_secret_longo_e_aleatorio
PORT=3000
