# electron-spike

Prova de conceito isolada, **não faz parte do app entregável**. Existe apenas para validar, antes do Sprint 1, se `electron-builder` empacota `better-sqlite3` sem dor (risco técnico #1 do [`PLANO-CONVERSAO-DESKTOP.md`](../docs/PLANO-CONVERSAO-DESKTOP.md), seção 7).

Depois que a validação for confirmada, este diretório deve ser removido ou ter seus aprendizados incorporados em `nodus-app/electron/` (caminho previsto pelo plano para `electron/main.ts` do US01) — não deve permanecer como um segundo projeto Electron paralelo ao real.
